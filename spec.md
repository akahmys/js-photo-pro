# JS PHOTO PRO 仕様書・要件定義書

## 1. プロジェクト概要
日本下水道事業団（JS）の「工事記録写真電子管理要領」に完全準拠した電子納品写真管理および `PHOTO.XML` 生成ツールです。サーバーとの通信を行わず、ブラウザの File System Access API を用いてユーザーのローカルPC上のファイル・フォルダを直接操作・編集する SPA（Single Page Application）として動作します。

## 2. 動作環境・技術スタック
- **動作環境**: File System Access API に対応した最新の PC 版ブラウザ（Google Chrome, Microsoft Edge など）
- **UIライブラリ**: React 18, ReactDOM 18 (Babel standalone を用いてブラウザ上で直接コンパイル・実行)
- **スタイリング**: Tailwind CSS (CDN), カスタム CSS (グラスモーフィズム等のモダンデザイン適用)
- **外部ライブラリ**:
  - `exifr`: 写真ファイルからの EXIF 日付自動抽出
  - `encoding-japanese`: `PHOTO.XML` 生成時の Shift_JIS エンコーディング変換

## 3. 主要機能と要件

### 3.1. プロジェクト（フォルダ）管理
- **新規プロジェクト作成**: 任意のローカルフォルダ内に、JS要領に準拠した `PHOTO`, `PIC`, `DRA` フォルダ構造と、初期の `PHOTO.XML`, `PHOTO05.DTD` を自動生成する機能。
- **既存フォルダ展開**: 既に存在する電子納品フォルダを読み込み、XMLを解析して写真データを画面に展開する機能（旧システムで作成されたXMLの読み込み・補正にも対応）。
- **プロジェクト結合**: 別の場所にある電子納品フォルダの写真を、現在開いているプロジェクトにマージ（追加）する機能。

### 3.2. 適用要領（基準）の切り替え
以下の2つの基準に対応し、切り替えによって適用される検証ルールやXMLタグ内の記述が変化します。
1. **H30年度基準** (H30.4.1 ～ R6.3.31 契約分)
   - 拡張子: 主に `.JPG` 
   - 適用要領基準: `土木201603-01`
2. **R6年度基準** (R6.4.1 以降 契約分)
   - 拡張子: `.JPEG`, `.SVG`, `.TIFF` 等を含む
   - 適用要領基準: `土木202303-01`

### 3.3. 写真のインポートと管理
- **ドラッグ＆ドロップ登録**: 画像ファイルをドラッグ＆ドロップするだけで即座に `PIC` フォルダへコピー配置し、アプリに反映する。
- **EXIF情報抽出**: インポート時に写真から撮影年月日を抽出し自動入力する。取得できない場合は空欄とし、操作者へ警告を促す。
- **ページネーション＆並べ替え**: 大量の写真（100件単位でページ分割）を管理可能。ドラッグ操作により表示順（シリアル番号）を自由に入れ替えでき、ページ間をまたいでの移動にも対応。

### 3.4. 属性の編集・バリデーション
- **一括編集機能**: `Ctrl`/`Shift` クリック等で複数写真を選択し、サイドバーから一括で属性（タイトル、区分、工種等）を変更できる。
- **マスター連携**: 区分や工種・種別・細別は、JS体系に準拠したマスターデータを保有しており、ドロップダウンやサジェストで入力漏れやミスを防ぐ。
- **リアルタイムバリデーション**: 工事種別（土木・建築等）、および写真区分（施工状況、着手前等）に応じて必須項目を動的に判定し、入力漏れがある場合はエラー件数やバッジで視覚的に警告する。

### 3.5. ファイル自動リネーム
- **最適化リネーム**: 画面上の表示順（シリアル番号）に従い、実際のファイル名を要領準拠のフォーマット（例: `P0000001.JPG`、参考図であれば `D0000001.JPG`）に一括リネーム・最適化する機能。

### 3.6. XML自動保存
- **オートセーブ**: 属性の変更、写真の追加、並べ替え等が行われるたびに、数秒間のディレイ（デバウンス処理）を挟んで自動的に `PHOTO.XML` (Shift_JIS形式) を `PHOTO` フォルダ内に上書き保存する。保存忘れを完全に防ぐ。

### 3.7. 印刷 (写真台帳出力)
- **ブラウザネイティブ印刷**: `window.print()` を利用して、JS要領に準拠したレイアウト（A4縦、1ページあたり写真2枚・各種メタデータ付き）で写真台帳を出力。PDFとしての保存等も可能。

## 4. データ構造 (PHOTO.XML)
出力されるXMLは、国交省の `PHOTO05.DTD` に完全に準拠して生成される。

**主要タグ構成:**
- `<photodata DTD_version="05">` : ルート要素
  - `<基礎情報>` : フォルダ定義 (PHOTO/PIC, PHOTO/DRA)、適用要領基準
  - `<写真情報>` (写真ごとに繰り返し)
    - `<写真ファイル情報>` : シリアル番号, 写真ファイル名, メディア番号(1固定)
    - `<撮影工種区分>` : 写真区分, 工種, 種別, 細別, 写真タイトル (大分類は "工事" 固定)
    - `<付加情報>` (任意) : 参考図ファイル名, 参考図タイトル
    - `<撮影情報>` : 撮影年月日 (CCYY-MM-DD固定)
    - `<代表写真>` / `<提出頻度写真>` : 0 または 1

## 5. UI/UX の特徴
- グラスモーフィズムやアニメーション（フェード、スピン等）をはじめとするモダンで操作性の高いデザイン。
- キーボード操作（Enterでの確定等）やIME入力（確定前の状態保持 `editDraft` パターン）に配慮した安全な入力フォームコントロール。
- プロセス処理中やXML生成時の「処理中オーバーレイ」および「トースト通知」による明確なフィードバック。
